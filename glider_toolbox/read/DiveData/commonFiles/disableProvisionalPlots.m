% **********************************************************************************************************************************
%
% **********************************************************************************************************************************
function [settings] = disableProvisionalPlots(handles, settings)
    % JES
    % DISABLE SPEED-OF-SOUND ON STARTUP. THIS IS CURRENTLY NOT A DELIVERABLE (EVEN THOUGH IT CAME TO THE RESCUE OF WESTERNGECO)
    % JES
 %   if exist('settings', 'var')
 %       settings.plotSelectionState(13) = 0;
 %   end

  %  strEval = sprintf( 'strState = get(handles.checkboxSelectPlot_%d, ''Enable'');', 13);
  %  eval(strEval);

   % if strcmp(strState, 'off') == 1
   %     strEval = sprintf( 'set(handles.checkboxSelectPlot_%d, ''Value'', %d);', 13, 0);
   %     eval(strEval);
   % end
end